// create function to encrypt password without bcrypt
function encryptPassword(password: string) {
    // create a method to encrypt password
    return password;
}
// create function to decrypt password without bcrypt
function decryptPassword(password: string) {
    return password;
}